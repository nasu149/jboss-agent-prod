[build-system]
requires = ["setuptools>=75", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "jboss-incident-agent"
version = "2.1.0"
description = "JBoss incident response demo using LangGraph, Gemini, MCP, ToolNode and Human-in-the-loop"
requires-python = ">=3.12,<3.14"
dependencies = [
  "langgraph>=1.1.5,<2",
  "langchain-google-genai>=4,<5",
  "langchain-mcp-adapters==0.3.2",
  "mcp[cli]>=1.24,<2",
  "streamlit>=1.45,<2",
  "pydantic>=2.10,<3",
  "python-dotenv>=1,<2",
  "httpx>=0.28,<1",
]

[project.optional-dependencies]
dev = [
  "pytest>=8,<9",
  "pytest-asyncio>=0.25,<1",
  "ruff>=0.11,<1",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.ruff]
line-length = 110
target-version = "py312"
